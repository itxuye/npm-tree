{
  "dependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.4.0",
    "@babel/preset-env": "^7.4.2",
    "axios": "^0.18.0",
    "fast-memoize": "^2.5.1",
    "lodash": "^4.17.11",
    "monet": "^0.9.0",
    "node-fetch": "^2.3.0",
    "restify": "^8.2.0"
  },
  "scripts": {
    "build": "rm -rf build && babel src -d build",
    "start": "npm run build && node build/index.js"
  }
}
